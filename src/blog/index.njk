---
layout: siteframe.njk
title: Blog
description: React developer, digital artist, and accessibility advocate.
---

{% from 'macros/breadcrumbs.macro.njk' import breadcrumbs %}

{{
  breadcrumbs([
    { url: "/", title: "Home" },
    { url: "/blog", title: "Blog" }
  ])
}}

<main id="main" class="contained-width">
  <header>
    <h1>Blog</h1>
    <p>
      Verbal scribbles about tech, web accessibility, art, and anything else.
    </p>
  </header>
  <ul>
    {% for post in collections.post | reverse %}
    <li>
      <article>
        <h3>
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </h3>
        <p>
          {{
            post.data.date.toLocaleDateString("en-us", {
              weekday: "long",
              year: "numeric",
              month: "long",
              day: "numeric"
            })
          }}
        </p>
      </article>
    </li>
    {% endfor %}
  </ul>
</main>
